# MindMesh Frontend

Lightweight Vite React + Tailwind frontend for the MindMesh AI journaling platform.

## Local Development

```bash
npm install
npm run dev -- --host 0.0.0.0 --port 8501
```

The app expects the backend at `VITE_API_BASE_URL`, defaulting to `http://localhost:8000`.
